name: w2w-essa


services:
  db:
    image: postgres:15
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7
    entrypoint: redis-server --requirepass ${REDIS_PASSWORD}
    env_file:
      - .env
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - redis_data:/data

  bot:
    image: cr.selcloud.ru/w2w/w2w-essa/app:development
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["./start.sh", "bot"]
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app
    volumes:
      - media:/app/media
    depends_on:
      - db
      - redis

  admin-panel:
    image: cr.selcloud.ru/w2w/w2w-essa/app:development
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["./start.sh", "admin_panel"]
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app
    volumes:
      - media:/app/media
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis

  celery:
    image: cr.selcloud.ru/w2w/w2w-essa/app:development
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: [ "./start.sh", "celery" ]
    env_file:
      - .env
    depends_on:
      - redis
      - db
      - admin-panel

volumes:
  postgres_data:
  redis_data:
  media: